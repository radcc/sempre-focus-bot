# Проверено в проде 24.08.2026 (Ubuntu 26.04, Python 3.14).
# Жёсткие пины (==) запрещены, кроме anthropic: у части библиотек нет
# готовых сборок (wheels) под cp314, pip падает на компиляции pydantic-core.
# anthropic==0.125.0 — код написан под этот SDK; 1.0+ ломает соединение.
# Миграция на anthropic 1.x — только отдельной задачей с тестами.
aiogram>=3.30
anthropic==0.125.0
httpx>=0.28
python-dotenv>=1.2
